allprojects {
    version = '1.2.2'
    repositories {
        mavenCentral()
        google()
    }
}

buildscript {
    apply from: 'dependencies.gradle'

    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath libraries.build.androidGradle
    }
}
